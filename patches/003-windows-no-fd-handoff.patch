diff --git a/packages/pty-daemon/src/Pty/Pty.ts b/packages/pty-daemon/src/Pty/Pty.ts
index 6bf8a83..55482c2 100644
--- a/packages/pty-daemon/src/Pty/Pty.ts
+++ b/packages/pty-daemon/src/Pty/Pty.ts
@@ -202,7 +202,9 @@ export function spawn({ meta }: SpawnOptions): Pty {
 	}
 	const adapter = new NodePtyAdapter(term, meta);
 	// Validate the private-fd dependency at spawn time, not handoff time.
-	adapter.getMasterFd();
+	// Windows (ConPTY) exposes no master fd, so fd-handoff is unsupported
+	// there and the assert must not run; prepareUpgrade refuses instead.
+	if (process.platform !== "win32") adapter.getMasterFd();
 	return adapter;
 }
 
diff --git a/packages/pty-daemon/src/Server/Server.ts b/packages/pty-daemon/src/Server/Server.ts
index 5b2db93..f219f4a 100644
--- a/packages/pty-daemon/src/Server/Server.ts
+++ b/packages/pty-daemon/src/Server/Server.ts
@@ -144,6 +144,14 @@ export class Server {
 	async prepareUpgrade(): Promise<
 		{ ok: true; successorPid: number } | { ok: false; reason: string }
 	> {
+		if (process.platform === "win32") {
+			// ConPTY has no inheritable master fd; sessions cannot survive a
+			// daemon swap on Windows. The supervisor falls back to a restart.
+			return {
+				ok: false,
+				reason: "fd-handoff is not supported on Windows (ConPTY has no master fd)",
+			};
+		}
 		const liveSessions = [...this.store.all()].filter((s) => !s.exited);
 		const fdIndexBySessionId = new Map<string, number>();
 
