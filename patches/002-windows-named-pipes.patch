diff --git a/apps/desktop/src/main/lib/terminal-host/client.ts b/apps/desktop/src/main/lib/terminal-host/client.ts
index 4916705..414c748 100644
--- a/apps/desktop/src/main/lib/terminal-host/client.ts
+++ b/apps/desktop/src/main/lib/terminal-host/client.ts
@@ -13,14 +13,14 @@ import { spawn, spawnSync } from "node:child_process";
 import { randomUUID } from "node:crypto";
 import { EventEmitter } from "node:events";
 import {
-	chmodSync,
+	chmodSync as _chmodSync,
 	closeSync,
-	existsSync,
+	existsSync as _existsSync,
 	mkdirSync,
 	openSync,
 	readFileSync,
 	statSync,
-	unlinkSync,
+	unlinkSync as _unlinkSync,
 	writeFileSync,
 } from "node:fs";
 import { connect, type Socket } from "node:net";
@@ -76,7 +76,27 @@ const DEBUG_CLIENT = process.env.SUPERSET_TERMINAL_DEBUG === "1";
 // Get from shared constants for multi-worktree support (imported at top of file)
 const SUPERSET_HOME_DIR = join(homedir(), SUPERSET_DIR_NAME);
 
-const SOCKET_PATH = join(SUPERSET_HOME_DIR, "terminal-host.sock");
+const SOCKET_PATH =
+	process.platform === "win32"
+		? `\\\\.\\pipe\\superset-terminal-host-${SUPERSET_DIR_NAME.replace(/[^A-Za-z0-9.-]/g, "-")}`
+		: join(SUPERSET_HOME_DIR, "terminal-host.sock");
+// Windows: filesystem unix sockets are unsupported (net.listen -> EACCES),
+// so use a named pipe. Pipes need no unlink/chmod, cannot be stale, and are
+// not reliably visible to fs.existsSync, so the fs helpers below treat any
+// \\.\pipe\ path accordingly.
+const isPipePath = (p: unknown): boolean =>
+	typeof p === "string" && p.startsWith("\\\\.\\pipe\\");
+const existsSync = (p: Parameters<typeof _existsSync>[0]): boolean =>
+	isPipePath(p) ? true : _existsSync(p);
+const unlinkSync = (p: Parameters<typeof _unlinkSync>[0]): void => {
+	if (!isPipePath(p)) _unlinkSync(p);
+};
+const chmodSync = (
+	p: Parameters<typeof _chmodSync>[0],
+	mode: Parameters<typeof _chmodSync>[1],
+): void => {
+	if (!isPipePath(p)) _chmodSync(p, mode);
+};
 const TOKEN_PATH = join(SUPERSET_HOME_DIR, "terminal-host.token");
 const PID_PATH = join(SUPERSET_HOME_DIR, "terminal-host.pid");
 const SPAWN_LOCK_PATH = join(SUPERSET_HOME_DIR, "terminal-host.spawn.lock");
diff --git a/apps/desktop/src/main/terminal-host/index.ts b/apps/desktop/src/main/terminal-host/index.ts
index ac5b158..ae517d6 100644
--- a/apps/desktop/src/main/terminal-host/index.ts
+++ b/apps/desktop/src/main/terminal-host/index.ts
@@ -14,11 +14,11 @@
 
 import { randomBytes } from "node:crypto";
 import {
-	chmodSync,
-	existsSync,
+	chmodSync as _chmodSync,
+	existsSync as _existsSync,
 	mkdirSync,
 	readFileSync,
-	unlinkSync,
+	unlinkSync as _unlinkSync,
 	writeFileSync,
 } from "node:fs";
 import { createServer, type Server, Socket } from "node:net";
@@ -60,7 +60,27 @@ const DAEMON_VERSION = "1.0.0";
 const SUPERSET_HOME_DIR = join(homedir(), SUPERSET_DIR_NAME);
 
 // Socket and token paths
-const SOCKET_PATH = join(SUPERSET_HOME_DIR, "terminal-host.sock");
+const SOCKET_PATH =
+	process.platform === "win32"
+		? `\\\\.\\pipe\\superset-terminal-host-${SUPERSET_DIR_NAME.replace(/[^A-Za-z0-9.-]/g, "-")}`
+		: join(SUPERSET_HOME_DIR, "terminal-host.sock");
+// Windows: filesystem unix sockets are unsupported (net.listen -> EACCES),
+// so use a named pipe. Pipes need no unlink/chmod, cannot be stale, and are
+// not reliably visible to fs.existsSync, so the fs helpers below treat any
+// \\.\pipe\ path accordingly.
+const isPipePath = (p: unknown): boolean =>
+	typeof p === "string" && p.startsWith("\\\\.\\pipe\\");
+const existsSync = (p: Parameters<typeof _existsSync>[0]): boolean =>
+	isPipePath(p) ? true : _existsSync(p);
+const unlinkSync = (p: Parameters<typeof _unlinkSync>[0]): void => {
+	if (!isPipePath(p)) _unlinkSync(p);
+};
+const chmodSync = (
+	p: Parameters<typeof _chmodSync>[0],
+	mode: Parameters<typeof _chmodSync>[1],
+): void => {
+	if (!isPipePath(p)) _chmodSync(p, mode);
+};
 const TOKEN_PATH = join(SUPERSET_HOME_DIR, "terminal-host.token");
 const PID_PATH = join(SUPERSET_HOME_DIR, "terminal-host.pid");
 
diff --git a/packages/host-service/src/daemon/DaemonSupervisor.ts b/packages/host-service/src/daemon/DaemonSupervisor.ts
index 780fac0..8e556b4 100644
--- a/packages/host-service/src/daemon/DaemonSupervisor.ts
+++ b/packages/host-service/src/daemon/DaemonSupervisor.ts
@@ -111,6 +111,10 @@ export function ptyDaemonSocketPath(organizationId: string): string {
 		.update(organizationId)
 		.digest("hex")
 		.slice(0, 12);
+	if (process.platform === "win32") {
+		// Filesystem unix sockets are unsupported on Windows; use a named pipe.
+		return `\\\\.\\pipe\\superset-ptyd-${shortId}`;
+	}
 	return path.join(os.tmpdir(), `superset-ptyd-${shortId}.sock`);
 }
 
@@ -1130,7 +1134,8 @@ async function waitForSocket(
 ): Promise<boolean> {
 	const deadline = Date.now() + timeoutMs;
 	while (Date.now() < deadline) {
-		if (fs.existsSync(socketPath)) {
+		// Named pipes are not reliably visible to fs.existsSync; probe directly.
+		if (socketPath.startsWith("\\\\.\\pipe\\") || fs.existsSync(socketPath)) {
 			if (await isSocketConnectable(socketPath, 200)) return true;
 		}
 		await new Promise((r) => setTimeout(r, 50));
diff --git a/packages/pty-daemon/src/Server/Server.ts b/packages/pty-daemon/src/Server/Server.ts
index 5b2db93..94896dd 100644
--- a/packages/pty-daemon/src/Server/Server.ts
+++ b/packages/pty-daemon/src/Server/Server.ts
@@ -64,13 +64,17 @@ export class Server {
 	}
 
 	async listen(): Promise<void> {
-		const dir = path.dirname(this.opts.socketPath);
-		fs.mkdirSync(dir, { recursive: true });
-		// Stale-socket cleanup: remove any prior socket file at this path.
-		try {
-			fs.unlinkSync(this.opts.socketPath);
-		} catch (err) {
-			if ((err as NodeJS.ErrnoException).code !== "ENOENT") throw err;
+		// Windows named pipes need no directory, stale cleanup, or chmod.
+		const isPipe = this.opts.socketPath.startsWith("\\\\.\\pipe\\");
+		if (!isPipe) {
+			const dir = path.dirname(this.opts.socketPath);
+			fs.mkdirSync(dir, { recursive: true });
+			// Stale-socket cleanup: remove any prior socket file at this path.
+			try {
+				fs.unlinkSync(this.opts.socketPath);
+			} catch (err) {
+				if ((err as NodeJS.ErrnoException).code !== "ENOENT") throw err;
+			}
 		}
 		await new Promise<void>((resolve, reject) => {
 			this.server.once("error", reject);
@@ -80,7 +84,7 @@ export class Server {
 			});
 		});
 		// Owner-only access. The socket file IS the auth boundary.
-		fs.chmodSync(this.opts.socketPath, 0o600);
+		if (!isPipe) fs.chmodSync(this.opts.socketPath, 0o600);
 	}
 
 	/**
